{
  "private": true,
  "scripts": {
    "deploy:raspberry": "pm2 deploy raspberry",
    "deploy:vps": "pm2 deploy vps",
    "lint": "eslint --ignore-path .eslintignore --ext .js .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lcdi2c": "^1.0.6",
    "ws": "^6.1.0"
  },
  "devDependencies": {
    "eslint": "^5.9.0"
  }
}
